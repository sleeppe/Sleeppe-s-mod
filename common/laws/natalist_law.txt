# Natalist Laws - 5 Tier System
# Progressively encourages population growth and birth rates

law_no_natalist_policy = {
	group = lawgroup_natalist_policy
	icon = "gfx/interface/icons/law_icons/no_natalist_policy.dds"

	progressiveness = -25

	modifier = {
	}

	ai_will_do = {
		always = no
	}

	ai_enact_weight_modifier = {
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_natalist_incentives = {
	group = lawgroup_natalist_policy
	icon = "gfx/interface/icons/law_icons/natalist_incentives.dds"

	progressiveness = 0

	unlocking_technologies = {
		
	}

	modifier = {
		
	}

	ai_will_do = {
		NOR = {
			has_law_or_variant = law_type:law_natalist_subsidies
			has_law_or_variant = law_type:law_natalist_mandate
			has_law_or_variant = law_type:law_aggressive_natalist_policy
		}
	}

	ai_enact_weight_modifier = {
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = { 
				country_population < 50000000
			}
			add = 200
		}
	}
}

law_natalist_subsidies = {
	group = lawgroup_natalist_policy
	icon = "gfx/interface/icons/law_icons/natalist_subsidies.dds"

	progressiveness = 25

	unlocking_technologies = {
		
	}

	modifier = {
		
	}

	ai_enact_weight_modifier = {
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = { 
				country_population < 50000000
			}
			add = 400
		}
	}
}

law_natalist_mandate = {
	group = lawgroup_natalist_policy
	icon = "gfx/interface/icons/law_icons/natalist_mandate.dds"

	progressiveness = 50

	unlocking_technologies = {
		
	}

	modifier = {
		
	}

	ai_enact_weight_modifier = {
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = { 
				country_population < 50000000
			}
			add = 600
		}

		if = {
			limit = {
				has_authority = authority_monarchy
			}
			add = 150
		}
	}
}

law_aggressive_natalist_policy = {
	group = lawgroup_natalist_policy
	icon = "gfx/interface/icons/law_icons/aggressive_natalist_policy.dds"

	progressiveness = 75

	unlocking_technologies = {
		
	}

	modifier = {
		state_birth_rate_mult = 0.10
		state_education_access_mult = 0.1
		interest_group_ig_rural_folk_pol_str_mult = 0.15
		interest_group_ig_devout_pol_str_mult = 0.10
		state_standard_of_living_add = -1
		state_tax_capacity_mult = -0.1
		state_welfare_payments_add = 0.1
		state_food_security_add = -0.1
	}

	ai_enact_weight_modifier = {
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = { 
				country_population < 50000000
			}
			add = 800
		}

		if = {
			limit = {
				NOR = {
					has_authority = authority_parliamentary_democracy
					has_authority = authority_liberal_democracy
					has_authority = authority_radicaldemocracy
				}
			}
			add = 250
		}
	}
}
