law_hereditary_bureaucrats = {
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/hereditary_bureaucrats.dds"

	progressiveness = -50

	is_visible = {
		NOR = {
			c:AUS ?= this # For the Austrian Empire and its subjects
			AND = {
				exists = c:AUS
				is_subject_of = c:AUS
			}
			c:CHI ?= this
		}
	}

	modifier = {
		state_bureaucracy_population_base_cost_factor_mult = -0.25
		country_aristocrats_pol_str_mult = 0.25
	}

	on_enact = {
		custom_tooltip = urban_aristocrats_int_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
	}

	disallowing_laws = {
		law_council_republic
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_traditionalist
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_crownland_diets = {
	group = lawgroup_bureaucracy
	parent = law_hereditary_bureaucrats # Inherits ideological stances from parent

	icon = "gfx/interface/icons/law_icons/crownland_diets.dds"

	progressiveness = -50

	is_visible = {
		OR = {
			c:AUS ?= this # For the Austrian Empire and its subjects
			AND = {
				exists = c:AUS
				is_subject_of = c:AUS
			}
		}
	}

	modifier = {
		state_bureaucracy_population_base_cost_factor_mult = -0.25
		country_aristocrats_pol_str_mult = 0.10
	}

	on_enact = {
		custom_tooltip = urban_aristocrats_int_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
	}

	disallowing_laws = {
		law_council_republic
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_traditionalist
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_chi_yinpriv = { 
	group = lawgroup_bureaucracy 
	parent = law_hereditary_bureaucrats

	icon = "gfx/interface/icons/law_icons/law_chi_enyin.dds"

	progressiveness = 0

	is_visible = {
		c:CHI ?= this
	}

	can_enact = {
		has_law_or_variant = law_type:law_monarchy
		has_law_or_variant = law_type:law_theocracy
	}

	modifier = {
		state_tax_capacity_mult = 0.25
		country_aristocrats_pol_str_mult = 0.50
		state_decree_cost_mult = 2.5
		country_consumption_tax_cost_mult = 3.0
		country_law_enactment_max_setbacks_add = -2
	}

	on_enact = {
		custom_tooltip = urban_aristocrats_int_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = {
		value = 0
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

}

law_appointed_bureaucrats = {
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/appointed_bureaucrats.dds"

	progressiveness = 0

	is_visible = { 
		NOT = {
			has_law = law_type:law_usa_spoilsystem
			c:CHI ?= this
		}
		}

	modifier = {
		country_bureaucrats_pol_str_mult = 0.25
		state_tax_capacity_mult = 0.25
	}

	on_enact = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				OR = {
					has_journal_entry = je_sick_man_bureaucracy
					has_journal_entry = je_great_reforms_bureaucratic
				}
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			add = 1000
		}
	}
}

law_usa_spoilsystem = { 
	group = lawgroup_bureaucracy 
	parent = law_appointed_bureaucrats 

	icon = "gfx/interface/icons/law_icons/law_usa_zhengzhifenfei.dds"

	progressiveness = 0 

	is_visible = { 
		has_law = law_type:law_usa_spoilsystem
		AND = {
			c:USA ?= this
			has_law_or_variant = law_type:law_elected_bureaucrats
		}
	}
	modifier = {
		country_bureaucracy_investment_cost_factor_mult = 0.40
		country_law_enactment_time_mult = -0.15
		state_decree_cost_mult = -0.10
	}

	on_enact = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = { 
		value = 0
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

}

law_elected_bureaucrats = {
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/elected_bureaucrats.dds"

	progressiveness = 50

	is_visible = { 
		NOT = {
			c:CHI ?= this
			c:USA ?= this
		}
	}

	disallowing_laws = {
		law_technocracy
		law_elder_council
	}

	on_enact = {
		custom_tooltip = bureaucrats_pb_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	modifier = {
		country_bureaucracy_investment_cost_factor_mult = -0.15
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

##modded

law_chi_purchaseof_office = { 
	group = lawgroup_bureaucracy 
	parent = law_appointed_bureaucrats 

	icon = "gfx/interface/icons/law_icons/law_chi_juanna.dds"

	progressiveness = 0 

	is_visible = { 
		c:CHI ?= this
	}

	disallowing_laws = {
		law_traditionalism
		law_industry_banned
		law_chi_physiocratic_policy
	}
	
	can_enact = { 
	}

	modifier = {
		country_bureaucracy_investment_cost_factor_mult = 0.4
		country_bureaucrats_pol_str_mult = 0.30
		state_tax_capacity_mult = 0.45
		state_decree_cost_mult = 1.5
		country_law_enactment_max_setbacks_add = -1
		country_consumption_tax_cost_mult = 0.15
	}

	on_enact = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = { 
		value = 0
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

}

law_dutch_provinces = {
	group = lawgroup_bureaucracy
	parent = law_appointed_bureaucrats

	icon = "gfx/interface/icons/law_icons/appointed_bureaucrats.dds"

	progressiveness = 0

	modifier = {
		country_bureaucrats_pol_str_mult = 0.35
		state_tax_capacity_mult = 0.15
		country_bureaucracy_investment_cost_factor_mult = -0.10
	}

	is_visible = {
		OR = {
			country_has_primary_culture = cu:dutch
			country_has_primary_culture = cu:wallonian
			country_has_primary_culture = cu:flemish
		}
	}

	on_enact = {
	}

	disallowing_laws = {
		law_council_republic
		law_corporate_state
		law_theocracy
		law_presidential_republic
		law_parliamentary_republic
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				OR = {
					has_journal_entry = je_sick_man_bureaucracy
					has_journal_entry = je_great_reforms_bureaucratic
				}
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			add = 1000
		}
	}
}

law_sejmik_poland = {
	group = lawgroup_bureaucracy
	parent = law_appointed_bureaucrats

	icon = "gfx/interface/icons/law_icons/appointed_bureaucrats.dds"

	progressiveness = 0

	modifier = {
		state_tax_capacity_mult = 0.05
		country_bureaucracy_investment_cost_factor_mult = -0.10
		state_bureaucracy_population_base_cost_factor_mult = 0.25
		country_voting_power_base_add = 15
		state_construction_mult	= 0.1
	}

	is_visible = {
		country_has_primary_culture = cu:polish
	}

	on_enact = {
		if = {
			limit = {
				has_law = law_type:law_autocracy
				has_law = law_type:law_technocracy
				has_law = law_type:law_oligarchy
				has_law = law_type:law_anarchy
			}
			activate_law = law_type:law_census_voting
		}
	}

	disallowing_laws = {
		law_autocracy
		law_technocracy
		law_oligarchy
		law_anarchy
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_usa_pendleton = { 
	group = lawgroup_bureaucracy 
	parent = law_elected_bureaucrats 

	icon = "gfx/interface/icons/law_icons/law_usa_pengdeerdunfa.dds"

	progressiveness = 0

	is_visible = { 
		c:USA ?= this
	}

	can_enact = {
	}
	unlocking_technologies = {
		philosophical_pragmatism
	}
	
	modifier = {
		country_bureaucracy_investment_cost_factor_mult = -0.15
		state_radicals_from_political_movements_mult = -0.10
	}

	on_enact = {
		custom_tooltip = bureaucrats_pb_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = {
		value = 0
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

}

law_chi_cadres = { 
	group = lawgroup_bureaucracy 
	parent = law_elected_bureaucrats 

	icon = "gfx/interface/icons/law_icons/law_chi_dangguanganbu.dds"

	progressiveness = 0 

	is_visible = { 
		c:CHI ?= this
	}

	can_enact = { 
		has_law_or_variant = law_type:law_council_republic 
	}

	modifier = {
		interest_group_ig_trade_unions_pol_str_mult = 0.25
		country_bureaucracy_investment_cost_factor_mult = -0.15
		country_bureaucracy_investment_cost_factor_mult = 0.10
		country_bureaucrats_pol_str_mult = 0.25
		state_tax_capacity_mult = 0.50
	}

	on_enact = {
		custom_tooltip = bureaucrats_pb_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = { 
		value = 0
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

}